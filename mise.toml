[tools]
node = "22"

# GITHUB_PACKAGES_TOKEN is set in mise.local.toml (gitignored), a PAT with
# read:packages scope, used by .npmrc to install @rogueserenity/kbdb-api-client
# from GitHub Packages.
